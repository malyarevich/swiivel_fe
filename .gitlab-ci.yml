stages:
  - build
  - deploy

variables:
  DEVELOPMENT_SERVER_USERNAME: devops
  DEVELOPMENT_SERVER_IP: 35.211.160.56

storybook_build:
  only:
    - storybook
  stage: build
  image: trion/ng-cli
  script:
    - rm -rf /builds/edminify/app-fe/node_modules/
    - df . -h
    - df -i
    - npm i --only=prod
    - npm run build-storybook
  artifacts:
    expire_in: 10 min
    paths:
      - storybook/

storybook_deploy:
  only:
    - storybook
  image: alpine
  stage: deploy
  before_script:
    - apk add --no-cache openssh
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" >> ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
  script:
    - apk add --no-cache rsync
    - rsync -rav --delete --rsync-path="sudo rsync" storybook/ $DEVELOPMENT_SERVER_USERNAME@$DEVELOPMENT_SERVER_IP:/var/www/app-fe/storybook/
