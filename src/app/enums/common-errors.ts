export enum COMMON_ERRORS {
    pattern = 'Field is invalid',
    required = 'Field is required',
    minlength = 'Field avctual length is %actualLength% . Minimal required length is %requiredLength%',
}
