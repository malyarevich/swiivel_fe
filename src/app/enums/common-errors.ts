export enum COMMON_ERRORS {
    pattern = 'Field is invalid',
    required = 'Field is required',
    minlength = 'Field actual length is %actualLength% . Minimal required length is %requiredLength%',
    maxlength = 'Field actual length is %actualLength% . Maximal required length is %requiredLength%',
}
